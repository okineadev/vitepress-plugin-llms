{
	"extends": "./tsconfig.base.json",
	"references": [{ "path": "./tsconfig.node.json" }],
	"compilerOptions": {
		"composite": true,
		"lib": ["DOM"],
		"types": ["bun-types"]
	},

	// Setup linting for tests later
	"include": ["tests/**/*.ts", "lint-staged.config.ts", "bunup.config.ts", "oxlint.config.ts"]
}
