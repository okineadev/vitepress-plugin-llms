name: CodeQL # zizmor: ignore[concurrency-limits]

on: [workflow_dispatch]

permissions: {}

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-slim

    concurrency:
      group: codeql-${{ matrix.language }}-${{ github.ref }}
      cancel-in-progress: true

    permissions:
      contents: read
      security-events: write # Required for uploading CodeQL security findings

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: javascript-typescript
            build-mode: none

    steps:
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          sparse-checkout-cone-mode: false
          persist-credentials: true
          sparse-checkout: |
            .github/workflows/
            src/

      - name: Initialize CodeQL
        uses: github/codeql-action/init@f0ac9bfbe331b3cc7be1482df79cca2865cb79b6
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@f0ac9bfbe331b3cc7be1482df79cca2865cb79b6
        with:
          category: '/language:${{matrix.language}}'
